snapshots:
    - name: survey_main_header_snapshot
      relation: ref('int_survey_main_header')
      config:
        schema: intermediate
        database: IVS_ANALYTICS_DWH
        unique_key: RESPONSE_ID
        strategy: timestamp
        updated_at: CREATED_AT
        dbt_valid_to_current: "to_date('9999-12-31')"